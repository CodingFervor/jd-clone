version: "3.9"

services:
  backend:
    build: ./backend
    container_name: jd-backend
    restart: unless-stopped
    environment:
      PORT: 8080
      DB_PATH: /app/data/app.db
      ALLOWED_ORIGINS: "*"
    ports:
      - "8080:8080"
    volumes:
      - jd_data:/app/data

  frontend:
    build: ./frontend
    container_name: jd-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  jd_data:
